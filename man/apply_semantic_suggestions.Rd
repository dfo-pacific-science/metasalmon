% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/semantics-helpers.R
\name{apply_semantic_suggestions}
\alias{apply_semantic_suggestions}
\title{Apply semantic suggestions into a dictionary}
\usage{
apply_semantic_suggestions(
  dict,
  suggestions = attr(dict, "semantic_suggestions"),
  strategy = "top",
  columns = NULL,
  roles = NULL,
  min_score = NULL,
  overwrite = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{dict}{A dictionary tibble, typically returned by \code{\link[=infer_dictionary]{infer_dictionary()}} or
\code{\link[=suggest_semantics]{suggest_semantics()}}.}

\item{suggestions}{A suggestions tibble, usually
\code{attr(dict, "semantic_suggestions")}. If omitted, the function reads that
attribute from \code{dict}.}

\item{strategy}{Selection strategy per column-role pair. Currently only
\code{"top"} is supported, which uses the first suggestion in each matched group.}

\item{columns}{Optional character vector limiting application to specific
\code{column_name} values.}

\item{roles}{Optional character vector limiting application to specific
suggestion roles: \code{"variable"}, \code{"property"}, \code{"entity"}, \code{"unit"},
\code{"constraint"}, \code{"method"}.}

\item{min_score}{Optional numeric threshold. Only available when
\code{suggestions} includes a \code{score} column; otherwise the function errors.}

\item{overwrite}{Logical; if \code{FALSE} (default), only missing fields are
filled. Set \code{TRUE} to intentionally replace existing IRIs.}

\item{verbose}{Logical; if \code{TRUE} (default), print a short summary.}
}
\value{
The dictionary tibble with selected semantic IRI fields filled in.
}
\description{
Copies selected IRIs from a \code{semantic_suggestions} tibble into the matching
dictionary fields. Suggestions remain separate by default; this helper gives
you an explicit merge step when you decide the top candidates are good enough.
}
\details{
Matching is done by both \code{column_name} and \code{dictionary_role}. When the
suggestions tibble also includes \code{dataset_id} and \code{table_id}, those keys are
honored too.
}
\examples{
\dontrun{
dict <- infer_dictionary(my_data, dataset_id = "example", table_id = "main")
dict <- suggest_semantics(my_data, dict)

# Fill only the missing semantic fields for one measurement column
dict <- apply_semantic_suggestions(dict, columns = "SPAWNER_COUNT")

# Require stronger lexical matches when score is available
dict <- apply_semantic_suggestions(dict, min_score = 2)
}
}
