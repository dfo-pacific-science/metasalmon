% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ontology_fetch.R
\name{parse_ontology_terms}
\alias{parse_ontology_terms}
\title{Parse ontology terms from a Turtle (TTL) file}
\usage{
parse_ontology_terms(ttl_path)
}
\arguments{
\item{ttl_path}{Path to a Turtle (.ttl) file.}
}
\value{
A tibble with columns:
\describe{
  \item{iri}{Full IRI of the term (character).}
  \item{local_name}{Local name without namespace prefix (character).}
  \item{label}{Human-readable label from \code{rdfs:label} or \code{skos:prefLabel} (character).}
  \item{definition}{Definition from \code{skos:definition} or \code{iao:0000115} (character).}
  \item{term_type}{OWL/SKOS type: \code{"owl:Class"}, \code{"skos:Concept"},
    \code{"skos:ConceptScheme"}, \code{"owl:ObjectProperty"},
    \code{"owl:DatatypeProperty"}, \code{"owl:AnnotationProperty"}, or
    \code{"other"} (character).}
}
}
\description{
Extracts term IRIs, labels, definitions, and types from a Turtle-format
ontology file. Designed for the DFO Salmon Ontology but works with any TTL
file that uses \code{rdfs:label}, \code{skos:prefLabel}, \code{skos:definition}, and/or
\code{iao:0000115} (IAO definition) annotation properties.
}
\details{
The parser is regex-based and does \strong{not} require an RDF library. It
resolves \code{gcdfo:} and \code{:} prefixed local names to full IRIs using the
\verb{@prefix} declarations found in the file.
}
\examples{
\dontrun{
ttl_path <- fetch_salmon_ontology()
terms <- parse_ontology_terms(ttl_path)
head(terms)
}
}
