% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/package-helpers.R
\name{create_sdp}
\alias{create_sdp}
\title{Create a Salmon Data Package directly from raw tables}
\usage{
create_sdp(
  resources,
  path = NULL,
  dataset_id = "dataset-1",
  table_id = "table_1",
  guess_types = TRUE,
  seed_semantics = TRUE,
  semantic_sources = c("smn", "gcdfo", "ols", "nvs"),
  semantic_max_per_role = 1,
  seed_verbose = TRUE,
  seed_codes = NULL,
  seed_table_meta = NULL,
  seed_dataset_meta = NULL,
  format = "csv",
  overwrite = FALSE,
  include_edh_xml = FALSE,
  edh_profile = c("dfo_edh_hnap", "iso19139"),
  edh_xml_path = NULL
)
}
\arguments{
\item{resources}{Either a named list of data frames (one per resource table)
or a single data frame (converted internally to a one-table list).}

\item{path}{Character; directory path where package will be written. If
omitted, defaults to \verb{file.path(getwd(), paste0(<dataset_id>-sdp))} using
a filesystem-safe dataset id slug.}

\item{dataset_id}{Dataset identifier applied to all inferred metadata rows.}

\item{table_id}{Fallback table identifier when \code{resources} is a single data frame.}

\item{guess_types}{Logical; if \code{TRUE} (default), infer \code{value_type} for each
dictionary column.}

\item{seed_semantics}{Logical; if \code{TRUE} (default), seed semantic suggestions
during inference.}

\item{semantic_sources}{Vector of vocabulary sources passed to
\code{suggest_semantics()}.}

\item{semantic_max_per_role}{Maximum number of suggestions retained per
I-ADOPT role.}

\item{seed_verbose}{Logical; if TRUE, emit progress messages while seeding
semantic suggestions.}

\item{seed_codes}{Optional \code{codes.csv}-style seed metadata.}

\item{seed_table_meta}{Optional \code{tables.csv}-style seed metadata.}

\item{seed_dataset_meta}{Optional \code{dataset.csv}-style seed metadata.}

\item{format}{Character; resource format: \code{"csv"} (default, only format supported)}

\item{overwrite}{Logical; if \code{FALSE} (default), errors if path exists}

\item{include_edh_xml}{Logical; when \code{TRUE}, writes an EDH XML metadata file into
the output package path using \code{edh_build_iso19139_xml()}.}

\item{edh_profile}{One of "dfo_edh_hnap" (default) or "iso19139". Determines
whether the richer HNAP-aware profile or compact fallback profile is written
when \code{include_edh_xml = TRUE}.}

\item{edh_xml_path}{Optional file path for the EDH output when
\code{include_edh_xml = TRUE}. If \code{NULL}, defaults to \code{metadata-edh-hnap.xml} for
\code{edh_profile = "dfo_edh_hnap"} and \code{metadata-iso19139.xml} for
\code{edh_profile = "iso19139"}.}
}
\value{
Invisibly returns the package path.
}
\description{
Primary one-shot wrapper: infer dictionary/table metadata/codes/dataset
metadata from raw data tables and immediately write a review-ready Salmon
Data Package.
}
\details{
This one-shot helper creates a review-ready package by default:
semantic suggestions are seeded and the top-ranked column-level suggestions
are auto-applied only into missing dictionary IRI fields. The output package
also includes \code{semantic_suggestions.csv} (when available) plus
\code{README-review.txt} with clear Excel-based review instructions, plus required-field review placeholders in the inferred metadata files.
}
\examples{
\dontrun{
data_path <- system.file("extdata", "nuseds-fraser-coho-sample.csv", package = "metasalmon")
fraser_coho <- readr::read_csv(data_path, show_col_types = FALSE)

pkg <- create_sdp(
  fraser_coho,
  dataset_id = "fraser-coho-2024",
  table_id = "escapement",
  overwrite = TRUE
)
}
}
