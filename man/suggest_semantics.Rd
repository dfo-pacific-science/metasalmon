% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/semantics-helpers.R
\name{suggest_semantics}
\alias{suggest_semantics}
\title{Suggest semantic annotations for a dictionary}
\usage{
suggest_semantics(
  df,
  dict,
  sources = c("smn", "gcdfo", "ols", "nvs"),
  include_dwc = FALSE,
  max_per_role = 3,
  search_fn = find_terms,
  codes = NULL,
  table_meta = NULL,
  dataset_meta = NULL
)
}
\arguments{
\item{df}{A data frame or tibble containing the data being documented.}

\item{dict}{A dictionary tibble created by \code{infer_dictionary()} (may have
incomplete semantic fields).}

\item{sources}{Character vector of vocabulary sources to search. Options are
\code{"smn"} (Salmon Domain Ontology via content negotiation), \code{"gcdfo"} (DFO-specific source), \code{"ols"} (Ontology Lookup Service), \code{"nvs"} (NERC Vocabulary Server), and
\code{"bioportal"} (requires \code{BIOPORTAL_APIKEY} environment variable).
Default is \code{c("smn", "gcdfo", "ols", "nvs")}.}

\item{include_dwc}{Logical; if \code{TRUE}, also attach DwC-DP export mappings
(via \code{suggest_dwc_mappings()}) as a parallel attribute \code{dwc_mappings}.
Default is \code{FALSE} to keep the UI simple for non-DwC users.}

\item{max_per_role}{Maximum number of suggestions to keep per I-ADOPT role
(variable, property, entity, unit, constraint) per column. Default is 3.}

\item{search_fn}{Function used to search terms. Defaults to \code{find_terms()}.
Can be replaced for testing or custom search strategies.}

\item{codes}{Optional \code{codes.csv}-like tibble. When provided, suggestions are
also generated for missing \code{codes.csv$term_iri} targets.}

\item{table_meta}{Optional \code{tables.csv}-like tibble. When provided,
suggestions are generated for missing \code{tables.csv$observation_unit_iri}.}

\item{dataset_meta}{Optional \code{dataset.csv}-like tibble. When provided,
suggestions are generated for missing \code{dataset.csv$keywords} as candidate
semantic keywords (IRIs intended for keyword curation).}
}
\value{
The dictionary tibble (unchanged) with a \code{semantic_suggestions}
attribute containing a tibble of suggested IRIs. The suggestions tibble
starts with \code{column_name}, \code{dictionary_role}, \code{table_id}, and \code{dataset_id}
so the original dictionary term is visible before the candidate match.
It also includes \code{target_scope}, \code{target_sdp_file}, and
\code{target_sdp_field} so users can see exactly where each accepted suggestion
would land in the Salmon Data Package. Additional columns include
\code{search_query}, \code{column_label}, \code{column_description}, \code{label}, \code{iri},
\code{source}, \code{ontology}, and \code{definition}. If the underlying search results
include a \code{score} column, it is preserved for downstream filtering.
For non-column targets, the tibble also includes explicit destination
context (\code{target_row_key}, \code{target_label}, \code{target_description},
\code{code_value}, \code{code_label}, \code{code_description}) so table-, dataset-, and
code-level rows are inspectable without extra joins.
}
\description{
Searches external vocabularies to suggest IRIs for semantic gaps in the
dictionary and package metadata. Measurement columns keep full I-ADOPT
decomposition (\code{term_iri}, \code{property_iri}, \code{entity_iri}, \code{unit_iri},
\code{constraint_iri}), while selected non-measurement columns can receive
lighter \code{term_iri} coverage when they are categorical or controlled
low-cardinality attributes.
}
\details{
The function uses the column's label or description as the search query and
returns suggestions as an attribute on the dictionary tibble. This allows
you to review candidates before accepting them into your dictionary.

Column targets keep full I-ADOPT behavior for
\code{column_role == "measurement"} rows. Non-measurement coverage is lighter:
only missing \code{term_iri} values are considered, focused on categorical rows
and controlled low-cardinality attribute rows inferred through \code{codes.csv}.
Identifier and temporal columns are skipped by default. When \code{codes},
\code{table_meta}, or \code{dataset_meta} are supplied, additional target rows are
generated for \code{codes.csv}, \code{tables.csv}, and \code{dataset.csv} respectively.

A term can legitimately appear more than once with different
\code{dictionary_role} values (for example as both a variable and a property).
In that case, \code{match_type} still describes lexical match quality, while
\code{target_sdp_field} tells you where that suggestion would be written in the
package. The output adds \code{role_collision} and \code{role_collision_note} so
variable-vs-property collisions stay explicit and destination-aware.

After calling this function, access suggestions with:

\if{html}{\out{<div class="sourceCode">}}\preformatted{suggestions <- attr(result, "semantic_suggestions")
}\if{html}{\out{</div>}}

Suggestions stay separate by default. Review them first, then use
\code{\link[=apply_semantic_suggestions]{apply_semantic_suggestions()}} for an explicit opt-in merge, or copy values
manually when you need finer control.
}
\examples{
\dontrun{
# Create a starter dictionary
dict <- infer_dictionary(my_data, dataset_id = "example", table_id = "main")

# Get semantic suggestions for measurement columns
dict_with_suggestions <- suggest_semantics(my_data, dict)

# View the suggestions
suggestions <- attr(dict_with_suggestions, "semantic_suggestions")
print(suggestions)

# Filter suggestions for a specific column
spawner_suggestions <- suggestions[suggestions$column_name == "SPAWNER_COUNT", ]

# Explicitly apply the top suggestion for one column without overwriting
# any existing IRIs in the dictionary
dict <- apply_semantic_suggestions(dict_with_suggestions, columns = "SPAWNER_COUNT")
}
}
\seealso{
\code{\link[=find_terms]{find_terms()}} for direct vocabulary searches, \code{\link[=infer_dictionary]{infer_dictionary()}}
for creating starter dictionaries, \code{\link[=apply_semantic_suggestions]{apply_semantic_suggestions()}} for
explicitly filling selected IRI fields, \code{\link[=validate_dictionary]{validate_dictionary()}} for
checking dictionary completeness.
}
