% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dictionary-helpers.R
\name{validate_dictionary}
\alias{validate_dictionary}
\title{Validate a salmon data dictionary}
\usage{
validate_dictionary(dict, require_iris = FALSE)
}
\arguments{
\item{dict}{A tibble or data.frame with dictionary schema columns}

\item{require_iris}{Logical; if \code{TRUE}, requires non-empty IRIs for semantic
fields (default: \code{FALSE}). Measurement columns always require \code{term_iri},
\code{property_iri}, \code{entity_iri}, and \code{unit_iri}.}
}
\value{
Invisibly returns the normalized dictionary if valid; otherwise
raises errors with clear messages
}
\description{
Validates a dictionary tibble against the salmon data package schema.
Checks required columns, value types, required flags, and optionally
validates IRIs. Reports issues using \code{cli} messaging.
}
\examples{
\dontrun{
dict <- infer_dictionary(mtcars)
validate_dictionary(dict)
}
}
