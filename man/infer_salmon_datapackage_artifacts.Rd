% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/package-helpers.R
\name{infer_salmon_datapackage_artifacts}
\alias{infer_salmon_datapackage_artifacts}
\title{Infer Salmon Data Package artifacts from resource tables}
\usage{
infer_salmon_datapackage_artifacts(
  resources,
  dataset_id = "dataset-1",
  table_id = "table_1",
  guess_types = TRUE,
  seed_semantics = TRUE,
  semantic_sources = c("smn", "gcdfo", "ols", "nvs"),
  semantic_max_per_role = 1,
  seed_verbose = TRUE,
  seed_codes = NULL,
  seed_table_meta = NULL,
  seed_dataset_meta = NULL,
  semantic_code_scope = c("factor", "all", "none")
)
}
\arguments{
\item{resources}{Either a named list of data frames (one per resource table)
or a single data frame (converted internally to a one-table list).}

\item{dataset_id}{Dataset identifier applied to all inferred metadata.}

\item{table_id}{Name used when \code{resources} is a single data frame.}

\item{guess_types}{Logical; if \code{TRUE} (default), infer \code{value_type} for each
dictionary column.}

\item{seed_semantics}{Logical; if \code{TRUE}, run
\code{suggest_semantics()} and attach semantic suggestions to the returned
dictionary.}

\item{semantic_sources}{Vector of vocabulary sources passed to
\code{suggest_semantics()}.}

\item{semantic_max_per_role}{Maximum number of suggestions retained per I-ADOPT
role.}

\item{seed_verbose}{Logical; if TRUE, emit progress messages while seeding
semantic suggestions.}

\item{seed_codes}{Optional \code{codes.csv}-style seed metadata.}

\item{seed_table_meta}{Optional \code{tables.csv}-style seed metadata.}

\item{seed_dataset_meta}{Optional \code{dataset.csv}-style seed metadata.}

\item{semantic_code_scope}{Character string controlling which \code{codes.csv}
rows are sent through \code{suggest_semantics()} during one-shot seeding.
\code{"factor"} (default) analyzes codes sourced from factor columns and
low-cardinality character columns in the original data frame(s); \code{"all"}
analyzes all inferred or supplied code rows; \code{"none"} skips code-level
semantic suggestions.}
}
\value{
A named list with the following components:
\itemize{
\item \code{resources}: Named list of input tables
\item \code{dict}: Inferred dictionary tibble
\item \code{table_meta}: Inferred table metadata tibble
\item \code{codes}: Inferred candidate codes tibble
\item \code{dataset_meta}: Inferred dataset metadata one-row tibble
\item \code{semantic_suggestions}: Semantic suggestion tibble (or \code{NULL})
}
}
\description{
Infers column dictionaries, table metadata, candidate code lists, and
dataset-level metadata in a single step from one or more raw data tables.
}
\details{
This is a convenience helper for biologists who want to get from raw
data frames to package-ready metadata artifacts with one call.
}
\examples{
\dontrun{
resources <- list(
  catches = data.frame(
    station_id = c("A", "B"),
    species = c("Coho", "Chinook"),
    count = c(10L, 20L),
    sample_date = as.Date(c("2024-01-01", "2024-01-02"))
  ),
  stations = data.frame(
    station_id = c("A", "B"),
    latitude = c(49.8, 49.9),
    longitude = c(-124.4, -124.5)
  )
)

artifacts <- infer_salmon_datapackage_artifacts(
  resources,
  dataset_id = "demo-1",
  seed_semantics = TRUE,
  seed_verbose = TRUE
)

dict <- artifacts$dict
table_meta <- artifacts$table_meta
codes <- artifacts$codes
dataset_meta <- artifacts$dataset_meta
}
}
